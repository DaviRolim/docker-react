language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t davirolim/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true davirolim/docker-react npm run test
